'use strict';

/**
 * @ngdoc function
 * @name wittyApp.controller:TermsCtrl
 * @description
 * # TermsCtrl
 * Controller of the wittyApp
 **/
angular.module('wittyApp').controller('TermsCtrl',
  function($http, $interval, $timeout, $location, $scope, Authentication, Profile, io, $cookies, $rootScope, $modal, $state, Users, Header, Notification, Projects, Beauty_encode, algolia) {



});
